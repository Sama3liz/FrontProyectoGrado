import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  blueText: {
    color: "blue",
  },
  redText: {
    color: "red",
  },
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
