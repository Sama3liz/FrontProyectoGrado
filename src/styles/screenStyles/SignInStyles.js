import { StyleSheet } from "react-native";

export const signInStyles = StyleSheet.create({
  root: {
    alignItems: 'center',
    padding: 20,
  },
  logo: {
    alignItems: 'center',
    width: '70%',
    maxWidth: 300,
    maxHeight: 200,
  },
});